require "spec_helper"

RSpec.describe Dice::Dsl do
  it "has a version number" do
    expect(Dice::Dsl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
